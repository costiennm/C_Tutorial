/* coment on
many lines */
#include <stdio.h>

int main(){
	int a, b, sum;
	printf("Nhap hai so bat ki:");
	scanf("%d%d", &a, &b);
	sum = a+b;
	printf("Tong cua hai so: %d + %d = %d", a, b, sum);
	return 0;
}
